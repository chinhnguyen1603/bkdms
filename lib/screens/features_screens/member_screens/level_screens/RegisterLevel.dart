import 'package:flutter/material.dart';

class RegisterLevel extends StatelessWidget {
  
  
  const RegisterLevel ({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}