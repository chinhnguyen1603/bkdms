import 'package:flutter/material.dart';

class CancelOrder extends StatelessWidget {

  const CancelOrder({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Đơn đã hủy/trả"),
    );
  }
}