import 'package:flutter/material.dart';

class Delivering extends StatelessWidget {

  const Delivering({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Đơn đang giao"),
    );
  }
}